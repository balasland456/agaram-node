export enum StatusCode {
    OK = 200,
    CREATED = 201,
    INTERNAL_SERVER_ERROR = 500,
    BAD_REQUEST = 400,
  }

export enum Status {
    ASSIGNED = "ASSIGNED",
    UNASSIGNED = "UNASSIGNED",
    COMPLETED = "COMPLETED",
    CLOSED = "CLOSED",
    REJECTED = "REJECTED"
}